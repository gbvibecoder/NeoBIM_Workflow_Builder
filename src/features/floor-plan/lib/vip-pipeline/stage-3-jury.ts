/**
 * Stage 3: Vision Jury
 *
 * Claude Sonnet evaluates all generated images and selects the best
 * one based on architectural quality, room coverage, and layout logic.
 *
 * Planned implementation: Phase 1.4
 */

import type { Stage3Input, Stage3Output } from "./types";

export async function runStage3VisionJury(
  input: Stage3Input,
): Promise<Stage3Output> {
  throw new Error("Stage 3 (Vision Jury) not implemented — Phase 1.4");
}
