/**
 * Stage 1: Prompt Intelligence
 *
 * Parses user prompt, enriches it into an architect brief, then
 * synthesizes model-specific image generation prompts (2–5 models).
 *
 * Planned implementation: Phase 1.2
 */

import type { Stage1Input, Stage1Output } from "./types";

export async function runStage1PromptIntelligence(
  input: Stage1Input,
): Promise<Stage1Output> {
  throw new Error("Stage 1 (Prompt Intelligence) not implemented — Phase 1.2");
}
