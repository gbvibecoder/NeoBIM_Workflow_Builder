"""Phase 1 — domain layer.

Pure-Python typed graph layer between the legacy MassingGeometry input
and the IFC builders. No IFC imports, no LLM calls, no DB calls. See
`building_model.py` for the schema and 12 invariants.
"""
