# Phase 1 — Deferred Items

Items discovered during Phase 1 execution that are out of scope for this branch. Candidates for Phases 2 and 3 (Validation/Dialog Agent + topology-first engine + transparency panel).

| # | Item | File:lines | Why deferred | Phase |
|---|------|-----------|--------------|-------|
| 1 | Pipeline B `B-mandala` coarse fallback derives `floorBoundary` from `Math.max(...rooms.boundary.points)` — i.e. plot becomes the bounding box of placed rooms, silently shrinking a 2600 ft² plot to 1528 ft² in the UI. | `src/features/floor-plan/lib/pipeline-b-orchestrator.ts:381-390` | Engine change. Phase 1 surfaces this via the new metrics (efficiency / area_deviation) instead of fixing the geometry. | 3 |
| 2 | `placeOpenings` is documented as "Contract: NEVER throws UNSAT" — silently skips doors when `findSharedEdgeWall` returns null. Phase 1 makes the missing doors visible (door_coverage metric) but does not change the placer. | `src/features/floor-plan/lib/csp-solver/opening-placer.ts:280-295, 434-463` | Engine change. Replacing graceful-degradation with re-solve loops is Phase 3. | 3 |
| 3 | `cell-csp.ts` value function rewards mandala proximity, exterior-wall touch, shared-edge adjacency — has no term for plot coverage / void minimization. This is the proximate cause of the floating-rooms pattern. | `src/features/floor-plan/lib/csp-solver/cell-csp.ts:395-430` | Engine change. Adding a `coverage` term or re-architecting around topology-first is Phase 3. | 3 |
| 4 | Pipeline router is binary A/B (signal threshold 5). No SPECIFIC / SEMI-SPECIFIC / GENERIC classification. Vastu adds +5 — essentially a hard switch to Pipeline B whenever the prompt mentions Vastu. | `src/features/floor-plan/lib/pipeline-router.ts:33-67` | Requires schema additions to the parser + UI changes around the Dialog Agent. Phase 2 work. | 2 |
| 5 | `infeasibility-detector.checkAreaImpossible` fires only at ratio > 1.2. Phase 1 added the `UNDER_FULL` warning at ratio < 0.65 as a non-blocking signal. The other suggested checks (perimeter shortage, adjacency cycles, dim > plot side) are still missing. | `src/features/floor-plan/lib/infeasibility-detector.ts:84-99` | Phase 2 — these become fields in the FeasibilityReport that drives the pre-generation Dialog Agent. | 2 |
| 6 | `EnhancedRoomProgram` is a different shape from `ParsedConstraints`. The Grid-First and BSP paths know `roomProgram` but not `parsedConstraints`, so `computeLayoutMetrics` runs on those paths without adjacency / dim deviation data — the metrics gracefully degrade rather than fail, but the surface is incomplete. | `src/app/api/generate-floor-plan/route.ts:335, 416, 484` (callers) | Building a converter `enhancedRoomProgramToParsedConstraints` is straightforward but adds surface area. Defer to Phase 2 alongside the unified extractor. | 2 |
| 7 | Pipeline B `feasibilityWarnings` are surfaced into the response but only Pipeline B populates them. Grid-First / BSP paths don't run `detectInfeasibility(constraints)` — they use `EnhancedRoomProgram`-shaped data instead. | `src/app/api/generate-floor-plan/route.ts` (Grid-First + BSP returns) | Same root cause as #6 — needs the unified extractor. Phase 2. | 2 |
| 8 | `getSampleProjectForPrompt` BHK-matched fallback still exists; Phase 1 only removed the silent automatic invocation. The function (and its samples) are still used by `loadSample()` and the explicit "Use a pre-built sample layout" button. Audit whether the per-BHK samples still match what the AI now produces; otherwise consider trimming. | `src/features/floor-plan/lib/sample-layouts.ts` | Sample-content audit — out of Phase 1 engine/transparency scope. | 2 or content task |
| 9 | The "Generated from" banner shows `originalPrompt` but does not show `relaxationsApplied` from Pipeline B. Those are still kept in `feedback.tips` only. | `src/features/floor-plan/components/FloorPlanViewer.tsx:559-575` | Will be replaced wholesale by the Phase 2 Transparency / Explain panel. | 2 |
| 10 | `design-quality-checker.computeDesignScore` is annotated as "design heuristics only" but is still consumed by the Coordinator's design-iteration loop. The Coordinator could benefit from also using `computeHonestScore`, but changing the loop's exit condition is engine-territory. | `src/app/api/generate-floor-plan/route.ts` (`runGridFirstPipeline` ~line 712) | Engine behavior change. Phase 3 — coordinate rewrite around topology-first planner. | 3 |
| 11 | The Quality tab in the right panel is hidden for non-pipeline data sources (sample / saved / blank). For "saved" projects we technically *could* recompute metrics by running `computeLayoutMetrics(project)` on load — defer the UX decision. | `src/features/floor-plan/components/panels/QualityPanel.tsx` | Small UX scope decision. Could ship in Phase 2. | 2 |
| 12 | GenerationLoader's `step` and `progress` props are now ignored. Real per-stage progress requires backend SSE / server-timing milestones from the multi-agent pipeline. | `src/features/floor-plan/components/GenerationLoader.tsx` | Phase 3 needs an SSE channel. Reserved props in place. | 3 |
| 13 | GN-004 now duplicates GN-012's Stage 1+2+3 inline. The brief allowed this for Phase 1 — Phase 2 should extract a shared `floor-plan-engine.ts` service that both handlers and the standalone route call. | `src/app/api/execute-node/handlers/gn-004.ts`, `gn-012.ts`, `src/app/api/generate-floor-plan/route.ts` | Bigger refactor. The current duplication is intentional for revertibility. | 2 |
| 14 | `FloorPlanViewer.handleGenerateFromPrompt` no longer drives `generationStep`/`generationProgress` on the store. Those fields are still populated by `startGeneration` (set to "working", 0). They could be removed from the store schema once we're sure no other consumer reads them. | `src/features/floor-plan/stores/floor-plan-store.ts:1733-1748` | Cleanup, not in Phase 1 scope. | 2 |
| 15 | `legacy constraint-solver.ts` is `@deprecated` per its own header but still imported via `layout-optimizer.ts`. Cleanup candidate. | `src/features/floor-plan/lib/constraint-solver.ts:3-5` | Phase 3 — remove with the engine rewrite. | 3 |
